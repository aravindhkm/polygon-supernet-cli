package chain

import (
	// "fmt"
	// "time"

)



// func getStakeIdInfo(nsArgs string) (*batchv1.Job, error) {
// 	return config.CLIENTSET.BatchV1().Jobs(nsArgs).Get(context.TODO(), "polygon-edge-job", metav1.GetOptions{})
// }

